# git-rust

## What is git-rust?
`git-rust` implements a minimal set of `git` main commands, which includes `init`, `cat-file`, `hash-object`, ...

## Usage
As simply as it looks, `git-rust` can be used through feeding `cargo run` directly with the wanted command.